---
---

<section class="relative min-h-screen flex items-center justify-center overflow-hidden bg-gradient-to-br from-cream via-amber-50 to-rose-50 pt-20">
  <!-- Floating Decorative Blobs -->
  <div class="absolute inset-0 overflow-hidden pointer-events-none">
    <!-- Big burgundy (mobile: far left, md+: original) -->
    <div
      class="absolute top-40 left-[-30%] w-72 h-72 bg-burgundy/20 rounded-full blur-md animate-float
             md:top-32 md:left-20">
    </div>

    <!-- Small gold (mobile: left edge lower, md+: original) -->
    <div
      class="absolute bottom-28 left-[-12%] w-40 h-40 bg-gold/30 rounded-full blur-sm animate-float-delayed
             md:bottom-40 md:left-10">
    </div>

    <!-- Tiny burgundy (mobile: left side mid, md+: original near title) -->
    <div
      class="absolute top-[55%] left-[4%] w-20 h-20 bg-burgundy/30 rounded-full blur-sm animate-pulse
             md:top-1/3 md:left-1/4">
    </div>

    <!-- Big gold right (mobile: push off to the right, md+: original) -->
    <div
      class="absolute top-28 right-[-35%] w-96 h-96 bg-gold/20 rounded-full blur-md animate-float
             md:top-48 md:right-20">
    </div>

    <!-- Medium burgundy right (mobile: keep to the right edge, md+: original) -->
    <div
      class="absolute bottom-24 right-[-20%] w-56 h-56 bg-burgundy/15 rounded-full blur-sm animate-float-delayed
             md:bottom-32 md:right-40">
    </div>

    <!-- Bottom-right gold (mobile: off the corner, md+: original) -->
    <div
      class="absolute -bottom-10 right-[-25%] w-80 h-80 bg-gold/25 rounded-full blur-md
             md:-bottom-20 md:-right-20">
    </div>
  </div>

  <div class="relative z-10 max-w-7xl mx-auto px-6 py-32 text-center">
    <h1 class="text-6xl md:text-8xl font-serif font-bold text-burgundy mb-6 leading-tight">
      Welcome to <span class="text-amber-700">Savore</span>
    </h1>

    <p class="text-xl md:text-2xl text-gray-700 mb-10 max-w-3xl mx-auto">
      Where Every Meal Tells a Story of Passion, Flavor, and Excellence
    </p>

    <div class="flex flex-col sm:flex-row gap-4 justify-center">
      <button class="bg-burgundy text-white px-8 py-4 rounded-lg text-lg font-semibold hover:bg-burgundy/90 transition shadow-lg">
        Reserve a Table
      </button>
      <button class="bg-white text-burgundy border-2 border-burgundy px-8 py-4 rounded-lg text-lg font-semibold hover:bg-burgundy hover:text-white transition">
        View Menu
      </button>
    </div>

    <!-- Featured Video/Image Section -->
    <div class="mt-20 relative">
      <div class="bg-white rounded-2xl shadow-2xl p-8 max-w-5xl mx-auto">
        <div class="bg-gradient-to-br from-gray-900 to-gray-800 rounded-xl overflow-hidden aspect-video flex items-center justify-center">
          <div class="text-center">
            <svg class="w-24 h-24 mx-auto text-gold mb-4" fill="currentColor" viewBox="0 0 24 24">
              <path d="M8 5v14l11-7z"/>
            </svg>
            <p class="text-white text-lg">Featured Restaurant Video</p>
            <p class="text-gray-400 text-sm mt-2">Experience our ambiance</p>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  @keyframes float {
    0%, 100% { transform: translateY(0) translateX(0); }
    33% { transform: translateY(-30px) translateX(20px); }
    66% { transform: translateY(20px) translateX(-20px); }
  }

  @keyframes float-delayed {
    0%, 100% { transform: translateY(0) translateX(0); }
    33% { transform: translateY(30px) translateX(-15px); }
    66% { transform: translateY(-20px) translateX(25px); }
  }

  .animate-float {
    animation: float 20s ease-in-out infinite;
  }

  .animate-float-delayed {
    animation: float-delayed 25s ease-in-out infinite;
  }
</style>
